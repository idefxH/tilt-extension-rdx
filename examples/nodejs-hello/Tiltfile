# Local dev: include the extension directly so changes to ../../rdx/Tiltfile
# are picked up without going through Tilt's repo-fetch cache.
include('../../rdx/Tiltfile')

rdx_app(
    name='hello',
    language='nodejs',
    port=8080,
    # services= defaults to None → auto-discovered from chart/values.yaml
    # (rdx-library.services[] DSL + legacy <chart>.enabled).
)
